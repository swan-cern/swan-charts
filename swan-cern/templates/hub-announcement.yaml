{{- if index .Values "enabled" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: hub-announcement
  namespace: {{ .Release.Namespace }}
data:
  announcement.json: {{ .Values.swanCern.announcement | default dict | toJson | quote }}
{{- end }}
